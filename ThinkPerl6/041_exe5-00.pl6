#!/usr/bin/env perl6
#
# As an exercise, write a compare subroutine that takes two numbers, $x and $y, and
# returns 1 if $x > $y, 0 if $x == $y, and -1 if $x < $y. Solution: “Exercise: Compare”
# on page 356.

